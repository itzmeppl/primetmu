export default function Upload() {
  // let upload_button = document.querySelectorAll(".upload-button");
  // console.log(upload_button);
  // console.log(upload_button[0]);
  // if (upload_button.classList.contains("hide")) {
  //   upload_button.classList.remove("hide");
  // }
  // upload_button[0].classList.add("hide");

  return (
    <div>
      <body></body>
    </div>
  );
}
